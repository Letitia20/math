# 第一问验证记录

验证日期：2026-09-10。

## 自动化测试

运行命令：

```powershell
$env:PYTHONPATH='src'
D:\miniconda3\python.exe -m pytest -q
```

结果：`13 passed`。测试覆盖扩散系数原式、附件插值、圆柱有限体积守恒、表面通量、边界方向、初值积分起点、结果采样、四位小数输出及二阶 Richardson 外推。

## 网格收敛

对 5120 与 10240 个径向区间，在完整的 1800×21 个输出节点比较：

| 变量 | 最大绝对差 |
|---|---:|
| 温度 | 0.00000103 °C |
| 水分浓度 | 0.00000457 kg/kg |

5120 与 10240 网格的直接四位小数结果仍分别有 1 个温度单元和 4 个水分单元发生末位翻转，说明“最大误差小于 0.00005”不能单独保证舍入稳定。最终提交数据采用两级解的二阶 Richardson 外推、BDF 时间积分、相对容差 `5e-10`、最大时间步 `1 s`。

另以 10240 与 20480 个区间对早期表面层和原舍入敏感点复算。两组 Richardson 外推在抽查点的最大差为温度 `2.25e-8 °C`、水分浓度 `7.64e-9 kg/kg`，四位小数逐项一致；其中 1 s、2 cm 为 `2.5177`，100 s、2 cm 为 `2.2470`。

## 工作簿检查

- 工作表名称及顺序：`温度`、`水分浓度`。
- 两个工作表均为 1801 行、22 列：1 行表头 + 1800 个整秒，1 列时间 + 21 个半径位置。
- 半径表头为 0--2 cm、步长 0.1 cm；时间为 1--1800 s。
- 所有场值均为有限数值单元格，显示格式为四位小数；冻结窗格为 `B2`。
- 将导出的 `.xlsx` 重新读入后，逐单元格与求解器 JSON 比较，完全一致。
- 工作簿中没有公式，因而不存在公式错误；重新导入后的公式扫描返回零记录。
- 已分别渲染两个工作表的前 25 个数据行，确认标题、列宽、数值显示和中文字体正常。

## 第二问

### 自动化测试

第二问现有 9 项测试，覆盖附录 3 四个物性公式、摄氏温度到开尔文转换、非物理输入拒绝、均匀平衡解、热湿边界方向、联立求解器向第一问物性退化的一致性、终点事件、断点续算，以及分段采样与一次性求解的一致性。

### 网格与时间控制

| 对照 | 温度最大绝对差/°C | 水分浓度最大绝对差/(kg/kg) |
|---|---:|---:|
| 2560--5120 区间，完整 10800×21 输出网格 | 0.000000814 | 0.000014937 |

原核查只抽取 0.5 h 以后的代表点，漏掉了最初几秒的表面边界层。最终采用 2560/5120 两级网格的二阶 Richardson 外推、相对容差 `2e-10`、最大时间步 2 s，并按 600 s 分段续算以控制内存。两级直接解在四位小数上各有 32 个温度单元和 32 个水分单元不一致；外推后 1 s、2 cm 水分浓度为 `2.5198`。10240 区间抽查值与外推原始值相差约 `1.25e-6`。

### 工作簿检查

- 工作表名称及顺序保持为 `温度`、`水分浓度`。
- 每张表为 10801 行、22 列：1 行表头 + 10800 个整秒，1 列时间 + 21 个半径位置。
- 工作簿无公式，公式错误扫描为零记录。
- 导出后重新读入，逐单元格与 `problem2_result.json` 比较完全一致。
- 两张表均已渲染检查，表头、四位小数、列宽和中文字体正常。
- 重新导入后的 453600 个场值与求解器 JSON 逐项一致；冻结窗格均为 `B2`。

## 第三问

### 自动化测试

新增终点事件、未达阈值异常、长期平台扩展、精确终点输出、终点 Richardson 外推及阈值局部插值测试。加入第二问断点续算和分段采样测试后，完整测试集共 27 项。

### 终点收敛

- 2560 区间：206918.229592 s；
- 5120 区间：206909.237679 s；
- 二阶外推并在共同场上重新定位：206906.244663 s，即 57.4739569 h；
- 1280/2560 与 2560/5120 外推结果相差约 1.02 s；
- 在 1280 区间把时间设置从 `2e-9/60 s` 收紧到 `2e-10/30 s`，终点变化 `1.9e-5 s`。

终点全域最大水分浓度为 0.15 kg/kg，位置为中心。所有终点前的常规输出时间均为 60 s 的整数倍，时间严格递增。

### 长期边界敏感性

采用最后 1 h 平台均值为基准。最后 0.5 h 均值、末值保持及平台均值上下一个标准差的 640 区间对照给出 57.2375--57.8259 h 的终点范围。该模型解释差异大于约 1.1 s 的数值不确定度。

### 工作簿检查

- `result3.xlsx` 只有模板规定的 `Sheet1`；
- 3450 行、22 列，其中 3448 个整分钟数据行和 1 个精确终点数据行；
- 21 个半径位置为 0--2 cm、步长 0.1 cm；
- 工作簿 75878 个数据单元与 JSON 结果逐项一致；
- 冻结窗格为 `B2`，未发现公式或公式错误。
