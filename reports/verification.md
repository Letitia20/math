# 第一问验证记录

验证日期：2026-09-10。

## 自动化测试

运行命令：

```powershell
$env:PYTHONPATH='src'
D:\miniconda3\python.exe -m pytest -q
```

结果：`11 passed`。测试覆盖扩散系数原式、附件插值、圆柱有限体积守恒、表面通量、边界方向、初值积分起点、结果采样与四位小数输出。

## 网格收敛

对 640 与 1280 个径向区间，在 7 个时刻和 5 个位置比较：

| 变量 | 最大绝对差 |
|---|---:|
| 温度 | 0.000001998 °C |
| 水分浓度 | 0.000021716 kg/kg |

最终提交数据采用 1280 个径向区间、BDF 时间积分、相对容差 `2e-9`、最大时间步 `2 s`。

## 工作簿检查

- 工作表名称及顺序：`温度`、`水分浓度`。
- 两个工作表均为 1801 行、22 列：1 行表头 + 1800 个整秒，1 列时间 + 21 个半径位置。
- 半径表头为 0--2 cm、步长 0.1 cm；时间为 1--1800 s。
- 所有场值均为有限数值单元格，显示格式为四位小数；冻结窗格为 `B2`。
- 将导出的 `.xlsx` 重新读入后，逐单元格与求解器 JSON 比较，完全一致。
- 工作簿中没有公式，因而不存在公式错误；重新导入后的公式扫描返回零记录。
- 已分别渲染两个工作表的前 25 个数据行，确认标题、列宽、数值显示和中文字体正常。
