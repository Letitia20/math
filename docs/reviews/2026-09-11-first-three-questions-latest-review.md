# 前三问最新版本复核

复核对象：远端 `codex/problem3` 最新提交 `871c872`，复核日期 2026-09-11。

## 结论

重新读取前三问的模型、运行脚本、测试、公式审计、验证报告和结果工作簿后，未发现会阻断第四问继续建模的错误。最新第三问已经正确区分“临界等于 0.15”与“严格小于 0.15”，并在 `result3.xlsx` 中保留临界行和下一整分钟严格达标行；这套口径已延续到第四问。

## 分问检查

### 第一问

- 题面扩散率 (D=7\times10^{-9}\exp(-0.89/C)) 的符号、单位和初值与原 PDF 一致。
- 圆柱有限体积离散、中心对称条件和表面 Robin 通量方向一致；热干环境方向性与封闭边界守恒均有自动化测试。
- `result1.xlsx` 两个工作表均为 1801 行、22 列，覆盖 1--1800 s 和 0--2 cm 的 21 个位置，不含公式。
- 正式结果采用 5120/10240 网格外推，解决了早期表面边界层导致的四位小数翻转问题。

### 第二问

- 附录 3 的 (ho(C),c_p(C),k(C),D(C,T)) 已逐式实现，其中 Arrhenius 项使用开尔文温度。
- 温度通过 (D(C,T)) 影响水分，含水率通过三项热物性影响温度，代码按同一状态向量联立积分。
- `result2.xlsx` 两个工作表均为 10801 行、22 列，覆盖 1--10800 s。题面“整个烘干过程”与模板只要求 3 h 的潜在歧义已在公式审计中明确：模型可继续积分，提交工作簿按模板保存前三小时。
- 表面传热、传质系数继续使用附录 2 数值是建模假设，现有文档没有把它误写成附录 3 给定值。

### 第三问

- 最新提交把终点定义为 (g(t)=\max_r C(r,t)-0.15) 的首次零点，报告 206906.2447 s（57.4739569 h）为严格条件的下确界。
- `result3.xlsx` 为 3451 行、22 列；末行 206940 s 是第一个整分钟严格达标时刻，未舍入中心含水率 0.1499901682 kg/kg。
- 2560/5120 网格的全过程质量—表面通量相对残差约 (1.21\times10^{-6})，且保存的径向场全程从中心向表面不增，因此“中心最湿”来自计算验证而不是预先假设。
- 长期烘房边界的不同解释给出约 57.24--57.83 h 的终点范围，文档正确指出该输入不确定性大于离散误差。

## 自动化与文件检查

- 在包含新增第四问测试的当前分支运行完整测试集：36 项全部通过；前三问原测试未发生回归。
- `result1.xlsx`、`result2.xlsx`、`result3.xlsx` 的工作表名称、时间范围、列数和公式数量已重新检查；三个文件都只含数值结果，没有公式。
- 本次复核没有修改前三问任何已有文件；本文是新增复核记录。

## 保留风险

前三问和第四问共同受三个模型假设限制：一维径向近似忽略端面，题面没有气固平衡关系，第四小时后的烘房边界需要延拓。因此当前结果可作为给定有效模型下的数值解，不应表述为经过独立实验验证的真实绝对误差。第四问应继续把传递系数沿用和边界延拓列入敏感性或局限说明。
